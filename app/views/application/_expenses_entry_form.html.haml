= simple_form_for @expenses_entry do |f|
  = f.error_notification
  = f.association :project, required: true, collection: @projects, label: t("entries.index.project")
  = f.input :amount, required: true, as: :integer, label:false, placeholder: t("entries.index.expense")
  = f.input :date, required: true, as: :string, input_html: { value: (@expenses_entry.date || DateTime.current).strftime("%d/%m/%Y"), class: "datepicker"}, label: false
  = f.input :supplier, required: true, as: :string, label: false, placeholder: t("entries.index.supplier")
  = f.input :description, required: true, as: :string, label: false, placeholder: t("entries.index.description")
  = f.input :currency, required: true, collection: currency_codes, label: false, placeholder: t("entries.index.currency")
  = f.input :exchangerate, as: :decimal,  hint: t("entries.index.exhangerate-hint"), placeholder: t("entries.index.exchangerate")
  = f.input :receipt, as: :file
  = f.button :submit, data: { disable_with: t("loader.saving") }
